// هذا كود تجريبي لعيون لونا 🌙✨

const logger = console; // حتى نشوف الكود يشتغل لو لا

export default {
    onLoad: () => {
        // هذا الكود يشتغل أول ما تشغلين البلوقن
        logger.log("Hello Luna! Plugin Loaded Successfully 💃🏻");
        
        // محاولة لإظهار رسالة بسيطة (اذا النظام سمح)
        try {
            const { showToast } = require("native"); // مكتبة الاشعارات
            showToast("هلاو لونا! البلوقن اشتغل ✅", 1); 
        } catch (e) {
            logger.error("ما كدرنا نطلع اشعار 😢", e);
        }
    },

    onUnload: () => {
        // هذا يشتغل من تطفين البلوقن
        logger.log("Bye Bye Luna! 👋🏻");
    }
}
